extends ../_component/layout/template

block cool-config
  -
      var myTitle = 'Learn to code HTML, CSS and JavaScript';
      var myDescription = 'This is an HTML replica of w3schools.com';
      var landingPage = '';
      var activeLink = '';

block awesome-content 
      h2 HTML Elements
      p An HTML element usually consists of a start tag and end tag, with the content inserted in between
      h2 HTML Attributes
      p Attributes provide additional information about HTML elements.
      h2 HTML Headings
      p Headings are defined with the <code> &lt;h1&gt; </code>  to <code> &lt;h6&gt; </code> tags.


